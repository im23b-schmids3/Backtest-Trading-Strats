---
name: smithers-hermes
description: "Add Smithers to a local Hermes agent: register the MCP server and install the native Hermes plugin (slash commands, live status line, approval buttons). Alias for `mcp add --agent hermes`. Run `smithers hermes --help` for usage details."
requires_bin: smithers
command: smithers hermes
---

# smithers hermes

Add Smithers to a local Hermes agent: register the MCP server and install the native Hermes plugin (slash commands, live status line, approval buttons). Alias for `mcp add --agent hermes`.
