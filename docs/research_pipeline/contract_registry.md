# Contract registry

The central registry is keyed by MBT, MET, MGC, SIL, MES, MNQ, and MCL. Each
typed contract record stores exchange, symbol, unit, minimum tick, tick value,
point value, currency, session definition, micro/mini relationship, source,
verification date, quality, and active status. Its deterministic hash is
persisted with reconciliation records.
