# Correlation and overlap

Overlap is measured from actual merged events and includes unique contribution,
duplicate exposure, simultaneous positions, and opposite conflicts. P&L
correlation is computed on aligned daily, weekly, and monthly periods plus
trade outcomes and drawdown overlap. Fewer than the configured minimum periods
produces `sufficient_evidence=false`, never a fabricated zero correlation.
