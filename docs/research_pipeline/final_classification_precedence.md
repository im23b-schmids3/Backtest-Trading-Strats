# Final classification precedence

Portfolio deferral never overrides an accepted standalone or portfolio
component result. Rejection and technical failure take precedence over
eligibility. The master report distinguishes `ACCEPTED_STANDALONE`,
`ACCEPTED_PORTFOLIO_COMPONENT`, `OWN_CAPITAL_ONLY`, `REJECTED`, insufficient
data, adapter failure, scope violation, and artifact-integrity failure.
