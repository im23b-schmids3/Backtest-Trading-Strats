# Final research classification

The final critic reconciles persisted evidence. Passing walk-forward and
holdout plus robust stress results can produce `ACCEPTED_STANDALONE` or
`ACCEPTED_PORTFOLIO_COMPONENT`; failed validation, execution sensitivity,
insufficient support, or unresolved review produces the corresponding
rejection/evidence state. No prose assertion overrides a failed gate.
