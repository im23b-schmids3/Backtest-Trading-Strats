# Phase E command reference

`portfolio eligible-strategies` lists eligible frozen members. Every portfolio
command returns JSON and exits nonzero on missing evidence, invalid transitions,
or budget exhaustion. Use `--registry PATH` before `portfolio` for an isolated
registry and `--repository-root PATH` for artifacts. The Smithers graph is
`.smithers/workflows/trading-research-phase-e.tsx`.
